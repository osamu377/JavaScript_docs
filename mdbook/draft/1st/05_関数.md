【やさしい日本語で学ぶ】JavaScriptの「関数（かんすう）」ガイド

皆さん、こんにちは。JavaScriptを勉強中の留学生の皆さんにとって、一番大切な道具（どうぐ）の一つが**「関数（かんすう）」**です。 関数を使いこなせると、プログラムを整理（せいり）して、賢（かしこ）く書くことができます。一緒に学んでいきましょう。


--------------------------------------------------------------------------------


1. はじめに：関数（かんすう）とは何か？

関数とは、いくつかの命令（めいれい）を一つにまとめた**「セット」のことです。イメージしやすいように、「自動販売機（じどうはんぱいき）」**の例えで考えてみましょう。

* お金を入れる・ボタンを押す： これが、関数に渡（わた）すデータ**「引数（ひきすう）」**です。
* 飲み物が出る： これが、命令を実行した結果（けっか）である**「戻り値（もどりち）」**です。

自動販売機の中には、「お金を数える」「在庫（ざいこ）を確認する」といった複雑（ふくざつ）なプログラムが入っています。でも、私たちは「ボタンを押す」という一つの命令だけで、欲しい結果を受け取ることができます。

プログラムをバラバラに書くのではなく、関数として「セット」にするメリットは2つあります。

1. 整理しやすくなる： 長いコードがスッキリします。
2. 再利用（さいりよう）できる： 同じことを何度も書かなくてよくなります。

次は、この便利な「関数」をどうやって作るのか、ルールを見ていきましょう。


--------------------------------------------------------------------------------


2. 関数の作り方（定義：ていぎ）

関数を新しく作ることを「定義（ていぎ）」と言います。 JavaScriptでは、誰が見ても分かりやすいように、名前の付け方に世界共通のルールがあります。

基本の書き方

以下のキーワードと記号を組み合わせて作ります。

* function： 「今から関数を作ります」という合図（あいづ）です。
* 関数名： そのセットに付ける名前です。
* ()： 引数（データ）を受け取るための場所です。
* {}： この中に、実行したい命令をすべて書きます。これを「ブロック」と呼びます。

良い名前をつけるルール

プログラマーは、関数が「何をするものか」すぐ分かるように、以下のルールを守ります。

1. 動詞（どうし）から始める： get（取る）、show（見せる）、calculate（計算する）、handle（処理する）などの動詞を使います。
2. キャメルケース（camelCase）： 2つ目以降の単語の最初を大文字にします。例：showMessage, calculateTotal

コードの例

挨拶（あいさつ）を<ruby>表示<rt>ひょうじ</rt></ruby>する簡単な関数です。

// 関数を定義する（準備）
function sayHello() {
  console.log("こんにちは！JavaScriptの世界へようこそ。");
}


これで「準備」ができました。次は、これを目的に合わせて「動かす」方法を説明します。


--------------------------------------------------------------------------------


3. 関数の呼び出し（実行：じっこう）

関数を作っただけでは、プログラムは<ruby>動<rt>うご</rt></ruby>きません。作った関数を動かすことを**「呼び出し」**と言います。初心者の方は「定義（準備）」と「呼び出し（実行）」を分けて考えることがとても大切です。

呼び出しの<ruby>仕組<rt>しく</rt></ruby>み

関数名のあとに () をつけると、中身が<ruby>動<rt>うご</rt></ruby>き出します。 sayHello(); と書くと、先ほど {} の中に書いたメッセージが<ruby>表示<rt>ひょうじ</rt></ruby>されます。

イベントとの連携（れんけい）

ウェブページでは、ボタンをクリックした時に関数を呼び出すことが多いです。これを**「イベントハンドラ」**と言います。

【HTMLとJavaScriptのつながり】

[ HTML側 ]
<button onclick="sayHello()">ボタンを押してね</button>
      ↓ 
(クリックされると、関数を呼び出す)
      ↓ 
[ JavaScript側 ]
function sayHello() {
    alert("こんにちは！");
}


引数（ひきすう）の活用と注意点

関数にデータを渡すことで、処理を柔軟（じゅうなん）に変えることができます。

* 仮引数（かりひきすう）： 関数の中で使う「データの箱」の名前。
* 実引数（じつひきすう）： 呼び出すときに実際に渡す「中身」のデータ。

⚠️ 留学生が気をつけるポイント： prompt() やフォームからデータを受け取るとき、それは必ず**「文字列（テキスト）」**になります。計算に使いたいときは、ソースにある Number() や parseInt() を使って、必ず「数値（すうじ）」に変換（へんかん）してから関数に渡しましょう。

次は、変数の「使える範囲」について学びます。


--------------------------------------------------------------------------------


4. 変数のスコープ（Scope）：変数の見える範囲

変数は、どこからでも見えるわけではありません。この「見える範囲」をスコープと呼びます。スコープを正しく使うことで、プログラムのミス（バグ）を減らすことができます。

スコープの種類	使える範囲	安全性と評価
グローバルスコープ	プログラム全体	危険（きけん）： どこでも値が変わってしまうため。
ブロックスコープ	{} の中だけ	安全： その場所だけで使うので、ミスが起きにくい。

var は使わない、const を使おう

JavaScriptには変数の作り方が3つありますが、安全性のために使い分けが重要です。

1. const (定数)： 【一番おすすめ】 一度決めたら変えられないので、最も安全です。
2. let (変数)： 値を変える必要があるときだけ使います。
3. var (古い書き方)： 【非推奨（ひすいしょう）】 範囲があいまいで、混乱（こんらん）やミスを招きやすいため、使わないようにしましょう。


--------------------------------------------------------------------------------


5. 無名関数（むめいかんすう）

名前を持たない関数のことを**「無名関数（むめいかんすう）」**と呼びます。 特定の場所で一度しか使わない処理（しょり）において、わざわざ名前をつけないことで、コードをスッキリさせることができます。

【無名関数の例】

// addEventListenerという機能の中で、直接関数を書いています
button.addEventListener("click", function() {
    console.log("名前のない関数が<ruby>動<rt>うご</rt></ruby>きました！");
});


名前をつけないことで、コンピューターのメモリを節約（せつやく）し、他のプログラムと名前がぶつかる心配もなくなります。とても効率的（こうりつてき）な書き方です。


--------------------------------------------------------------------------------


6. 関数のネスト（入れ子構造）

関数の中に、さらに関数を書くことができます。これを**「ネスト（入れ子）」と呼びます。これは、プログラムを整理して情報を守る「カプセル化（情報の保護）」**に役立ちます。

親子関係とスコープ

これはDOMツリーの「親要素（div）」と「子要素（p）」の関係に似ています。

* ルール： 内側の関数（子供）は、外側の関数（親）にある変数を見ることができます。
* 例え： 「子供は自分の家の冷蔵庫（親の変数）を開けて中身を見ることができます。でも、外にいる隣の人（外の関数）は、その家の中を見ることはできません。」

このように構造（こうぞう）を作ることで、大切なデータを外から勝手に触られないように守ることができます。


--------------------------------------------------------------------------------


7. まとめ

JavaScriptの「関数」をマスターするために、5つのポイントを振り返りましょう。

* 定義： function を使い、動詞から始まる名前をつけてセットにしましょう。
* 呼び出し： 関数名のあとの () を忘れずにつけて実行しましょう。
* スコープ： 変数は const を使い、なるべく {} の中で安全に管理しましょう。
* 無名関数： 一度きりの処理は、名前をつけずにスマートに書きましょう。
* ネスト： 関数を入れ子にして、大切な情報を守りながら整理しましょう。

So What?（だから何？） 関数を学ぶことは、ただコードを書くことではありません。大きな問題（プログラム）を、小さくて扱いやすい「関数のセット」に分けて考えることです。これが**「プログラマーのように考える」**ための第一歩です。

最初は難しく感じるかもしれませんが、自動販売機を作るエンジニアになったつもりで、たくさん練習してみてください。応援しています！
